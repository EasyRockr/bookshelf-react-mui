export const divSpace = (t) => ({ my: 2 })
export const centerRow = (t) => ({
  display: 'grid',
  placeItems: 'center',
  minHeight: 120,
})
export const tagRow = (t) => ({
  display: 'flex',
  flexWrap: 'wrap',
  gap: 0.75,
})
