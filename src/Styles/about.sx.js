export const heroCardSx = (t) => ({
  maxWidth: 860,
  mx: 'auto',
  my: 3,
  textAlign: 'center',
})

export const avatarSx = (t) => ({
  width: 100,
  height: 100,
  mx: 'auto',
  mb: 1,
})

export const chipRowSx = (t) => ({
  display: 'flex',
  flexWrap: 'wrap',
  gap: 1,
  justifyContent: 'center',
  mt: 1,
})

export const subCardSx = (t) => ({
  height: '100%',
})

export const mt2 = { mt: 2 }
